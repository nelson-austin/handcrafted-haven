// USER FIELDS id, name, email, password, is_seller, business_name
export type User = {
    id: string;
    name: string;
    email: string;
    password: string;
    is_seller: boolean;
    business_name: string;
  };